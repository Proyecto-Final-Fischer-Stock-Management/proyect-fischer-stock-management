const styles = {
  backgroundImage: "url('/mingcute_menu-fill.png')",
  width: "25px",
  height: "25px",
};

function MenuButton() {
  return <button style={styles}></button>;
}

export default MenuButton;
